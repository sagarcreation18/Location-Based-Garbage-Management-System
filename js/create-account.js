(() => {
  const $ = selector => document.querySelector(selector);
  const toast = (type, title, message) => { const icons = { success: 'fa-circle-check', error: 'fa-circle-exclamation', info: 'fa-circle-info' }; const item = document.createElement('div'); item.className = `app-toast ${type}`; item.innerHTML = `<i class="fa-solid ${icons[type] || icons.info}"></i><div><b>${title}</b><span>${message}</span></div>`; $('#toast-stack').append(item); setTimeout(() => { item.classList.add('hide'); setTimeout(() => item.remove(), 350); }, 3500); };
  const redirect = role => ({ citizen: 'citizen-dashboard/index.html', driver: 'driver-dashboard/index.html' })[String(role).toLowerCase()];
  const theme = $('.theme-toggle'); theme.addEventListener('click', () => { document.body.classList.toggle('dark'); theme.querySelector('i').className = document.body.classList.contains('dark') ? 'fa-solid fa-sun' : 'fa-solid fa-moon'; });
  document.querySelectorAll('.signup-toggle').forEach(button => button.addEventListener('click', () => { const input = button.parentElement.querySelector('input'); input.type = input.type === 'password' ? 'text' : 'password'; button.querySelector('i').className = input.type === 'password' ? 'fa-regular fa-eye' : 'fa-regular fa-eye-slash'; }));
  $('#signup-form').addEventListener('submit', async event => { event.preventDefault(); const form = event.currentTarget; const password = $('#signup-password').value; if (!form.checkValidity() || password !== $('#confirm-password').value) { form.classList.add('was-validated'); if (password !== $('#confirm-password').value) toast('error', 'Passwords do not match', 'Enter the same password in both fields.'); return; } const button = form.querySelector('button[type="submit"]'); button.disabled = true; try { const response = await fetch('http://localhost:5000/api/auth/register', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ full_name: $('#full-name').value.trim(), email: $('#signup-email').value.trim(), phone: $('#signup-phone').value, password, role: document.querySelector('input[name="account-role"]:checked').value }) }); const result = await response.json(); if (!response.ok) throw new Error(result.message || 'Unable to create account'); localStorage.setItem('ecotech-token', result.token); localStorage.setItem('ecotech-user', JSON.stringify(result.user)); const destination = redirect(result.user.role); if (!destination) throw new Error('Account created, but no portal is available for this role.'); toast('success', 'Account created', 'Opening your EcoSmart workspace...'); setTimeout(() => { location.href = destination; }, 500); } catch (error) { toast('error', 'Account creation failed', error.message); } finally { button.disabled = false; } });
})();
