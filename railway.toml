[build]
builder = "RAILPACK"
buildCommand = "cd backend && npm ci"

[deploy]
preDeployCommand = "cd backend && npm run migrate:production"
startCommand = "cd backend && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
